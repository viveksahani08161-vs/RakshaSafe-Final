# RakshaSafe AI service environment configuration (development)
# Copy to .env and adjust as needed.

SERVICE_NAME=rakshasafe-ai-service
VERSION=0.1.0
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Allowed origins as a JSON array (pydantic-settings list format)
CORS_ORIGINS=["http://localhost:5173"]